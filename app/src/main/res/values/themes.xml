<resources xmlns:tools="http://schemas.android.com/tools">
    <attr name="customBackgroundColor" format="color" />
    <attr name="customButtonBackgroundColor" format="reference" />

    <!-- Base application theme. -->
    <style name="Theme.ClotheingSuggester.Day" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="android:colorBackground">@color/colorBackgroundLight</item>
        <item name="android:textColor">@color/colorTextTitle</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="customBackgroundColor">@drawable/home_background_day</item>
        <item name="customButtonBackgroundColor">@color/homeCardLightColor</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Theme.ClotheingSuggester.Night" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="android:colorBackground">@color/colorBackgroundDark</item>
        <item name="android:textColor">@color/colorTextTitle</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->\
        <item name="customBackgroundColor">@drawable/home_background_night</item>
        <item name="customButtonBackgroundColor">@color/homeCardDarkColor</item>
        <item name="android:fitsSystemWindows">true</item>

        <!-- Splash Screen-->
        <item name="android:windowBackground">@mipmap/ic_launcher_round</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>